// Vercel's build command for this project runs this script before `next build`.
// It was originally generated by the v0.app deploy pipeline; that pipeline
// doesn't run for pushes made outside v0.app, so this no-op stub keeps
// `node .v0/inject-built-with-v0.mjs && next build` working either way.
